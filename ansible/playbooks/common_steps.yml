---
- hosts: "{{ target  }}"
  # Gather_facts le dice a ansible que no recoja informaciond el servidor
  gather_facts: false
  remote_user: root
  become: true
  roles:
      - update_upgrade 
      - install_common_packages
      - create_users
      - restart_server
